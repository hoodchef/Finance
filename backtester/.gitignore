node_modules
.next
out
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.cache

# Recorded vendor price data. Tiingo's licence is personal-use only, so these
# are NOT redistributable — regenerate them with your own key:
#   npm run record:fixtures
tests/fixtures/tiingo-*.json
